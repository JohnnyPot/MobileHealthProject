import { AnimateSlidingItemDirective } from './animate-sliding-item.directive';

describe('AnimateSlidingItemDirective', () => {
  it('should create an instance', () => {
    const directive = new AnimateSlidingItemDirective();
    expect(directive).toBeTruthy();
  });
});
